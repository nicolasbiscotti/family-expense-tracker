# Environment Configuration Example
# Copy this to .env.local and fill in your Firebase values

# Environment: local | preview | production
VITE_APP_ENV=local

# Firebase Configuration
# Get these values from Firebase Console > Project Settings > Your apps
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# Emulator Configuration (only for local development)
VITE_USE_EMULATORS=true
VITE_EMULATOR_AUTH_PORT=9099
VITE_EMULATOR_FIRESTORE_PORT=8080
VITE_EMULATOR_STORAGE_PORT=9199
VITE_EMULATOR_FUNCTIONS_PORT=5001

# Firestore Root Path (for environment isolation)
# Leave empty for local (emulators are already isolated)
# Use "environments/preview" for preview
# Use "environments/production" for production
VITE_FIRESTORE_ROOT_PATH=
